## Items for startup
* Install zsh
* Install om-my-zsh
* `chsh -s /bin/zsh` Make zsh default
* git clone dotfiles
* `sh install.sh`
* Install emacs
* Install Cask
* git clone .emacs.d
* `cask install Cask`

